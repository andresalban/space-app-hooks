import React from 'react';

const Cargando = () => {
  return(
      <div style={{display: "flex", justifyContent: "center", alignItems: "center", height: "100vh"}}>
          <img src="img/loading.gif" alt="loading"  style={{width:'30vw'}} />
      </div>

  )
}
export default Cargando;